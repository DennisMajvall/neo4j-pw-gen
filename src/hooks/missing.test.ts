import { useCopyToClipboard } from "./use-copy-to-clipboard";

test("useCopyToClipboard should attempt to write to navigator.clipboard when called", () => {
  // TODO: set up spy, check "toBeCalledWith" etc.
  // But don't have enough spare time to do this right now
});
